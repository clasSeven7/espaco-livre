// import { Given, Then, When } from '@cucumber/cucumber';

// Given('que o usuário está na tela de cadastro de Espaços', async () => {
//   // Aqui você pode navegar ou garantir que está na página, se precisar
//   console.log('Usuário acessou a tela de cadastro de Espaços');
// });

// When('ele preenche o campo {string}', async (campo: string) => {
//   console.log(`Preencheu o campo: ${campo}`);
// });

// When('ele adiciona fotos no campo {string}', async (campo: string) => {
//   console.log(`Adicionou fotos no campo: ${campo}`);
// });

// When(
//   'ele configura os {string} personalizando dias e horários',
//   async (campo: string) => {
//     console.log(`Configurou os horários disponíveis: ${campo}`);
//   }
// );

// When('ele seleciona os {string}', async (campo: string) => {
//   console.log(`Selecionou métodos de pagamento: ${campo}`);
// });

// When('ele clica no botão {string}', async (botao: string) => {
//   console.log(`Clicou no botão: ${botao}`);
// });

// Then('o sistema deve salvar o novo espaço', async () => {
//   console.log('Espaço salvo com sucesso');
// });

// Then(
//   'o sistema deve exibir uma mensagem de sucesso {string}',
//   async (mensagem: string) => {
//     console.log(`Mensagem exibida: ${mensagem}`);
//   }
// );

// // -------------- CENÁRIO DE DADOS INVÁLIDOS ----------------

// When('ele não preenche o campo {string}', async (campo: string) => {
//   console.log(`Campo não preenchido: ${campo}`);
// });

// When('ele não adiciona fotos no campo {string}', async (campo: string) => {
//   console.log(`Nenhuma foto adicionada no campo: ${campo}`);
// });

// When(
//   'ele não configura os {string} personalizando dias e horários',
//   async (campo: string) => {
//     console.log(`Horários não configurados: ${campo}`);
//   }
// );

// When('ele não seleciona os {string}', async (campo: string) => {
//   console.log(`Nenhum método de pagamento selecionado: ${campo}`);
// });

// Then(
//   'o sistema deve exibir mensagens de erro para cada campo obrigatório',
//   async () => {
//     console.log('Mensagens de erro exibidas para os campos obrigatórios');
//   }
// );
